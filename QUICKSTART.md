# 🚀 Lumina Reader — Тез Бастау Нұсқаулығы

Бұл нұсқаулық сізге жобаны **5 минутта** іске қосуға көмектеседі.

---

## 📋 Алдын ала талаптар

Компьютеріңізде орнатылған болуы керек:

| Бағдарлама | Тексеру командасы | Жүктеу сілтемесі |
|------------|-------------------|------------------|
| **Python 3.8+** | `python --version` | [python.org](https://www.python.org/downloads/) |
| **Node.js 16+** | `node --version` | [nodejs.org](https://nodejs.org/) |
| **Tor Browser** | — | [torproject.org](https://www.torproject.org/download/) |

---

## ⚡ Қадамдық нұсқаулық

### 1-қадам: Жобаны клондау және папкаға өту

```bash
git clone https://github.com/your-username/fl-reader.git
cd fl-reader
```

### 2-қадам: Python виртуалды ортасын жасау (ұсынылады)

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 3-қадам: Python тәуелділіктерін орнату

```bash
pip install -r requirements.txt
```

### 4-қадам: .env файлын жасау

```bash
# .env.example файлын көшіріңіз
copy .env.example .env    # Windows
cp .env.example .env      # Linux/Mac
```

> ⚠️ **МАҢЫЗДЫ:** .env файлында `SECRET_KEY` мәнін өзгертіңіз!

### 5-қадам: Node.js тәуелділіктерін орнату

```bash
npm install
```

### 6-қадам: Tailwind CSS компиляциялау ⚠️ МІНДЕТТІ!

```bash
npm run build:css
```

> ❌ Егер бұл қадамды өткізіп жіберсеңіз, сайт дизайны бұзылады!

### 7-қадам: Дерекқорды дайындау

```bash
python manage.py migrate
python manage.py createsuperuser
```

> Суперпайдаланушы жасағанда логин мен құпия сөзді есте сақтаңыз!

### 8-қадам: Tor-ды іске қосу (бөлек терминалда)

```bash
# Windows (Tor Browser орнатылған болса)
& "C:\Users\<USERNAME>\Desktop\Tor Browser\Browser\TorBrowser\Tor\tor.exe"

# Linux/Mac
tor
```

> ✅ "Bootstrapped 100% (done): Done" хабарламасын көргенше күтіңіз!

### 9-қадам: Django серверін іске қосу

```bash
python manage.py runserver
```

### 10-қадам: Браузерде ашу

```
http://127.0.0.1:8000/
```

---

## 🎉 Дайын!

Енді сіз:
- Жүйеге кіре аласыз (login)
- Флибустадан кітап іздей аласыз
- Кітаптарды жүктеп, оқи аласыз

---

## ❓ Жиі кездесетін қателер

### 1. "Сайт дизайны бұзылған / CSS жоқ"

**Себебі:** Tailwind CSS компиляцияланбаған.

**Шешімі:**
```bash
npm install
npm run build:css
```

### 2. "Ошибка поиска на Флибусте: WinError 10061"

**Себебі:** Tor іске қосылмаған немесе порт қате.

**Шешімі:**
1. Tor-дың іске қосылғанын тексеріңіз:
   ```bash
   & "C:\Users\<USERNAME>\Desktop\Tor Browser\Browser\TorBrowser\Tor\tor.exe"
   ```
2. `.env` файлында портты тексеріңіз:
   - Tor Browser қолдансаңыз: `TOR_PROXY_PORT=9150`
   - Терминалда tor.exe қолдансаңыз: `TOR_PROXY_PORT=9050`

3. Django серверін **қайта іске қосыңыз** (Ctrl+C, содан кейін қайта `python manage.py runserver`)

### 3. "No module named 'xxx'"

**Себебі:** Python тәуелділіктері орнатылмаған.

**Шешімі:**
```bash
pip install -r requirements.txt
```

### 4. "Кітаптар барлық пайдаланушыларға ортақ"

**Себебі:** Ескі дерекқор құрылымы.

**Шешімі:**
```bash
# Дерекқорды тазалап, қайта жасау
rm db.sqlite3
rm books/migrations/0*.py
python manage.py makemigrations books
python manage.py migrate
python manage.py createsuperuser
```

---

## 📁 Жоба құрылымы

```
fl-reader/
├── .env                 # Конфигурация (құпия, git-қа кірмейді)
├── .env.example         # Конфигурация үлгісі
├── requirements.txt     # Python тәуелділіктері
├── package.json         # Node.js тәуелділіктері
├── manage.py            # Django басқару скрипті
├── db.sqlite3           # Дерекқор (git-қа кірмейді)
│
├── config/              # Django конфигурациясы
├── books/               # Негізгі қосымша
│   ├── models.py        # Book моделі (user-ге байланысты)
│   ├── views.py         # View функциялары
│   ├── urls.py          # URL маршруттары
│   ├── services/        # Flibusta және FB2 қызметтері
│   └── templates/       # HTML шаблондары
│
├── static/              # Статикалық файлдар
│   ├── css/
│   │   ├── input.css    # Tailwind кіріс файлы
│   │   └── output.css   # Компиляцияланған CSS
│   └── js/
│
└── media/               # Жүктелген файлдар
    ├── books/           # FB2 файлдары
    └── covers/          # Мұқаба суреттері
```

---

## 🔧 Пайдалы командалар

| Команда | Сипаттама |
|---------|-----------|
| `npm run build:css` | Tailwind CSS компиляциялау |
| `npm run watch:css` | CSS өзгерістерін автоматты бақылау |
| `python manage.py runserver` | Django серверін іске қосу |
| `python manage.py createsuperuser` | Админ жасау |
| `python manage.py migrate` | Дерекқор миграциясы |
| `python manage.py makemigrations` | Жаңа миграция жасау |

---

## 📞 Қолдау

Егер қате шықса:
1. Осы нұсқаулықты қайта оқыңыз
2. Жоғарыдағы "Жиі кездесетін қателер" бөлімін тексеріңіз
3. GitHub Issues-қа жазыңыз

